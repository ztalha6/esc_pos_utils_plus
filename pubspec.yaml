name: esc_pos_utils_plus_forked
description: Basic Flutter/Dart classes for ESC/POS printing. Ticket class generates ESC/POS commands that can be sent to a thermal printer.
version: 3.0.0
homepage: https://github.com/ztalha6/esc_pos_utils_plus

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^4.1.7
  html: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - packages/esc_pos_utils_plus/resources/capabilities.json
